import Layout from '../components/MyLayout';

export default function Index() {
  return (
    <Layout>
      <p>Welcome to a next level web service! This web service runs on serverless platform, serves API and a blazing fast UI with features such as server side rendering, code splitting!</p>
    </Layout>
  );
}
